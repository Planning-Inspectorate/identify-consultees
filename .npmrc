# Match Azure Pipelines npm behaviour (legacy-peer-deps defaults to false).
# A user-global legacy-peer-deps=true hides the preact 8 vs 10 peer conflict
# (accessible-autocomplete vs @defra/interactive-map) that breaks Azure `npm ci`.
legacy-peer-deps=false
fund=false
audit=false
